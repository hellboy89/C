#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, soma;

    printf("Digite o valor de X: ");
    scanf("%d", &x);

    printf("Digite o vlaor de Y: ");
    scanf("%d", &y);

    soma = x + y;

    printf("\nSoma = %d\n", soma);

    return 0;
}
