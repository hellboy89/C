#include <stdio.h>
// Abaixo a função "include" é onde tem as bibliotecas de funções da linguagem.
// Abaixo é o chamado entre-pontos, todo o meu programa está localizado entre os colchetes abaixo.
// No fim da instrução é comum utilizar o "return 0" serve para caso dê algum erro após a execução, não mostrar.
// Se caso quiser que mostre, necessário mudar o "return".
int main() {
    printf("Ola mundo");

    return 0;

}
// Ao executar um programa em C, ele irá criar mais 2 arquivos no diretório, um será com extensão .exe,
// E o outro com extensão .o, que será o compilador.
// Se quiser também possível abrir o programa com extensão .exe, sem usar o code blocks.
// Sendo que ele abre e fecha rápido demais se executar somente ele, teria que ter um pause.
// Ou utilize o prompt de comando ou powershell que irá abrir certinho.
